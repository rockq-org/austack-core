(function () {
  'use strict';
/* jshint ignore:start */
  angular
    .module('<%- moduleName %>'<% if (deps) { %>, <%= JSON.stringify(deps) %><% } %>)<% constants.forEach(function(constant) { %>
.constant('<%- constant.name %>', <%= constant.value %>)
<% }) %>;
/* jshint ignore:end */
})();